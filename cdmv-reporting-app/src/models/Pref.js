class Pref {

    constructor(index, columnName, sort, filter, hidden, width) {
        this.index = index;
        this.columnName = columnName;
        this.sort = sort;
        this.filter = filter;
        this.width = width;
        this.hidden = hidden;
    }
}
export default Pref;