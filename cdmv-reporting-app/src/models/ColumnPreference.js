class ColumnPreference {

    constructor(field, header, sortable, filter, width, hidden) {
        this.field = field;
        this.header = header;
        this.sortable = sortable;
        this.filter = filter;
        this.width = width;
        this.hidden = hidden;
    }
}
export default ColumnPreference;